name: nordbite
description: Premium restaurant discovery POC for Sweden
version: 1.0.0

environment:
  sdk: ^3.7.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: any
  firebase_auth: any
  cloud_firestore: any
  http: any
  flutter_riverpod: any
  google_fonts: any
  shimmer: any
  flutter_animate: any
  cached_network_image: any
  url_launcher: any
  web: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
